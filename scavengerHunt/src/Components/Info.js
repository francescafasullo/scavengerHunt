import React, { Component } from 'react'
import { AppRegistry, View, Text, StyleSheet, Button } from 'react-native'

export default class Info extends Component {
    constructor(props) {
        super(props)
    }

    render() {
        return (
            {/*information about specific map pin locations like a restaurant*/}
        )
    }
}

AppRegistry.registerComponent('scavengerHunt', () => InfoScreen)