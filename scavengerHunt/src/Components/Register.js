import React, { Component } from 'react'
import { AppRegistry, View, Text, StyleSheet, Button } from 'react-native'

export default class Register extends Component {
    constructor(props) {
        super(props)
    }

    render() {
        return (
            {/*Choice to sign up or login if no user is signed in*/}
        )
    }
}

AppRegistry.registerComponent('scavengerHunt', () => RegisterScreen)