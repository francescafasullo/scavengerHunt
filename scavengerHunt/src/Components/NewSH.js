import React, { Component } from 'react'
import { AppRegistry, View, Text, StyleSheet, Button } from 'react-native'

export default class NewSH extends Component {
    constructor(props) {
        super(props)
    }

    render() {
        return (
            {/*map with ability to pin and add tokens*/}
        )
    }
}

AppRegistry.registerComponent('scavengerHunt', () => NewSHScreen)